<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found | Sneaker Head</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .error-page {
            min-height: 80vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: var(--spacing-3xl) 0;
            margin-top: 80px;
        }

        .error-content {
            max-width: 600px;
        }

        .error-animation {
            position: relative;
            margin-bottom: var(--spacing-3xl);
        }

        .error-shoe {
            font-size: 4rem;
            color: var(--accent-primary);
            margin-bottom: var(--spacing-lg);
            animation: float 3s ease-in-out infinite;
        }

        .error-number {
            font-size: clamp(6rem, 15vw, 12rem);
            font-weight: 800;
            color: var(--text-muted);
            opacity: 0.3;
            line-height: 1;
            margin-bottom: var(--spacing-lg);
        }

        .error-title {
            font-size: clamp(2rem, 5vw, 3rem);
            margin-bottom: var(--spacing-lg);
            color: var(--text-primary);
        }

        .error-description {
            font-size: 1.125rem;
            color: var(--text-secondary);
            margin-bottom: var(--spacing-2xl);
            line-height: 1.6;
        }

        .error-actions {
            display: flex;
            gap: var(--spacing-lg);
            justify-content: center;
            margin-bottom: var(--spacing-3xl);
            flex-wrap: wrap;
        }

        .error-suggestions {
            background: var(--bg-secondary);
            padding: var(--spacing-xl);
            border-radius: var(--radius-lg);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .error-suggestions h3 {
            margin-bottom: var(--spacing-lg);
            color: var(--text-primary);
        }

        .suggestion-links {
            display: flex;
            gap: var(--spacing-lg);
            justify-content: center;
            flex-wrap: wrap;
        }

        .suggestion-links a {
            color: var(--accent-primary);
            text-decoration: none;
            padding: var(--spacing-sm) var(--spacing-md);
            border-radius: var(--radius-sm);
            border: 1px solid var(--accent-primary);
            transition: all var(--transition-normal);
        }

        .suggestion-links a:hover {
            background: var(--accent-primary);
            color: var(--bg-primary);
        }

        @media (max-width: 768px) {
            .error-actions {
                flex-direction: column;
                align-items: center;
            }
            
            .suggestion-links {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    
    <section class="error-page">
        <div class="container">
            <div class="error-content" data-aos="fade-up">
                <div class="error-animation">
                    <div class="floating-shoe error-shoe">
                        <i class="fas fa-shoe-prints"></i>
                    </div>
                    <div class="error-number">404</div>
                </div>
                
                <h1 class="error-title">Oops! Page Not Found</h1>
                <p class="error-description">
                    Looks like this page took a wrong turn. Don't worry, even the best shoes 
                    sometimes wander off the beaten path.
                </p>
                
                <div class="error-actions">
                    <a href="/" class="btn btn-primary btn-large">
                        <i class="fas fa-home"></i>
                        <span>Back to Home</span>
                    </a>
                    <a href="/products" class="btn btn-outline btn-large">
                        <i class="fas fa-shopping-bag"></i>
                        <span>Shop Now</span>
                    </a>
                </div>
                
                <div class="error-suggestions">
                    <h3>Popular Pages</h3>
                    <div class="suggestion-links">
                        <a href="/products?category=running">Running Shoes</a>
                        <a href="/products?category=casual">Casual Wear</a>
                        <a href="/about">Our Story</a>
                        <a href="/contact">Contact Us</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>
    <script src="/js/main.js"></script>
</body>
</html>